#pragma once

class Pawn : public ChessPiece  {
    bool canMove() override {
        //If starting postion; first or second forward box needs to be empty
        //else first forward box needs to empty
    }
};
